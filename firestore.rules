rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Explicitly mirror emulator default-open behavior for local development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
